// Package mcpserver exposes GitContribute application capabilities through the
// Model Context Protocol.
//
// Reader tools and resources are local and offline. Operator tools declare
// network reads, local writes, durable jobs, or process execution through
// distinct interfaces and MCP annotations. Protocol payloads are mapped to
// product-owned application contracts rather than implementing use cases here.
package mcpserver
