{
  "version": "agent-tool-eval-oracle.v2",
  "fixture_revision": "agent-contract-v5",
  "scenarios": [
    {
      "id": "newest_music_thread",
      "correct_conclusion": "Issue 2 is newest.",
      "required_evidence": ["A stored thread search uses sort=updated.", "The returned source_updated_at orders issue 2 ahead of issue 1."],
      "critical_discriminator": "Default relevance ranks the older title match first; newest intent requires explicit updated ordering.",
      "tempting_wrong_path": "Use default relevance and call its first result newest.",
      "acceptable_tools": ["corpus.search_threads", "corpus.get_threads"],
      "minimum_semantic_score": 0.8
    },
    {
      "id": "readme_evidence_boundary",
      "correct_conclusion": "The indexed README documents MIDI support, subject to code-index coverage.",
      "required_evidence": ["README evidence comes from corpus.search_code, not repository metadata ranking.", "The response reports whether the selected code snapshot was truncated."],
      "critical_discriminator": "Repository description absence is not evidence that README documentation is absent.",
      "tempting_wrong_path": "Search only repository names and descriptions, then answer no.",
      "acceptable_tools": ["corpus.search_code"],
      "minimum_semantic_score": 0.8
    },
    {
      "id": "persisted_dossier_boundary",
      "correct_conclusion": "Return the stored dossier and its persisted as-of value without regeneration.",
      "required_evidence": ["corpus.get_repository_dossier is used.", "No build, sync, or GitHub tool is called."],
      "critical_discriminator": "The current repository projection can be newer than the persisted dossier; a read must not silently rebuild it.",
      "tempting_wrong_path": "Call corpus.build_repository_dossier before reading.",
      "acceptable_tools": ["corpus.get_repository_dossier"],
      "forbidden_tools": ["corpus.build_repository_dossier", "github.sync_repository_context"],
      "minimum_semantic_score": 0.8
    },
    {
      "id": "repository_dossier_availability",
      "correct_conclusion": "Return one ordered repository batch containing stored metadata and dossier availability, while qualifying unavailable repositories.",
      "required_evidence": ["corpus.get_repositories receives all three repository references in one bounded call.", "Complete items report dossier_status and dossier_as_of when available.", "Unavailable items retain item-level status and recovery guidance."],
      "critical_discriminator": "Comparing dossier availability does not require loading each full dossier or performing any write or network operation.",
      "tempting_wrong_path": "Fan out three corpus.get_repository_dossier calls and retry not-found results with build or sync tools.",
      "acceptable_tools": ["corpus.get_repositories"],
      "forbidden_tools": ["corpus.get_repository_dossier", "corpus.build_repository_dossier", "github.sync_repository_context"],
      "expected_max_tool_calls": 1,
      "minimum_semantic_score": 0.8
    },
    {
      "id": "exact_issue_set_preparation",
      "correct_conclusion": "Return one offline issue-set evidence result whose linkage remains advisory and whose missing evidence has explicit recovery actions.",
      "required_evidence": ["workflow.prepare_issue_set receives the three exact issue numbers in one bounded call.", "Issue and relationship coverage qualify absence claims.", "Every linkage candidate requires confirmation from the implementation."],
      "critical_discriminator": "Known issue scope does not require durable opportunities, draft rendering, or separate thread, coverage, and precedent calls.",
      "tempting_wrong_path": "Create opportunities or manually compose corpus.get_threads, corpus.get_coverage, and corpus.find_precedents before preparing a draft.",
      "acceptable_tools": ["workflow.prepare_issue_set"],
      "forbidden_tools": ["corpus.get_threads", "corpus.get_coverage", "corpus.find_precedents", "workflow.prepare_contribution"],
      "expected_max_tool_calls": 1,
      "minimum_semantic_score": 0.8
    }
  ]
}
